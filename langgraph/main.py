from doc_pipeline.graph import agent, create_graph

__all__ = ["agent", "create_graph"]

if __name__ == "__main__":
    print("Graph entrypoint ready. Use LangSmith/LangGraph UI to run the pipeline.")
